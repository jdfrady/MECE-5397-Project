%% This code compares ....

%% User Inputs
t = 3;   % Simulation Time
nt = 5;  % Number of time steps
ny = 3;  % Number of y steps 
nx = 3;  % Number of x steps
ax = 0;  % Value of lower x limit
bx = 0;  % Value of upper x limit
ay = 0;  % Value of lower y limit
by = 0;  % Value of upper y limit

%% Definitions and Initializatons
dx = (bx-ax)/(nx+1);          % X step size
dy = (by-ay)/(ny+1);          % Y step Size
dt = t/(nt+1);                % Time step size
u = zeros(nx+1, ny+1, nt+1);  % Allocating memory 


%% Boundary Condidions
temp 1


%% Explicit Calculation



%% Exact Solution


%% Visualization

